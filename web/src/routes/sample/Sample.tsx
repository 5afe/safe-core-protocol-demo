import { FunctionComponent } from "react";
import "./Sample.css";

type SampleProps = {
};

export const Sample: FunctionComponent<SampleProps> = ({ }) => {
    
    return (
        <div className="SamplePlugin">
        </div>
    );
};
